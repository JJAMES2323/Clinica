// script.js

function toggleForm() {
    var registrationForm = document.getElementById("registration-form");
    registrationForm.classList.toggle("hidden");
  }
  
  function submitForm() {
    // Aquí puedes agregar lógica para enviar el formulario al servidor si es necesario
    // Puedes utilizar AJAX, Fetch API o simplemente enviar el formulario con el método POST
    // Dependiendo de tus necesidades y configuración del backend.
  }
  